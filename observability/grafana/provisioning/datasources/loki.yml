apiVersion: 1

datasources:
  - name: Loki
    uid: loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: false
    jsonData:
      maxLines: 1000
      # Explore: корректная работа с volume API Loki (при volume_enabled в loki.yml)
      timeout: 60
